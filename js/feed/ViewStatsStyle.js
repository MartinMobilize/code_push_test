import {StyleSheet} from 'react-native';

const ViewStatsStyles = StyleSheet.create({
    statsRow:{
        flex: 1,
        marginTop:10,
        flexDirection: 'row',
    },

})

export default ViewStatsStyles;